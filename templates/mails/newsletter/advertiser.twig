{% extends layout %}

{% import "mails/macros.twig" as e %}

{% set settings = config.get %}

{% block body %}

    {% apply markdown_email %}

        {{ data.subject }}
        ===============================

        {% if format == 'html' %}
            {{ e.section(data.content | markdown_untrusted) }}
        {% endif %}
    {% endapply %}

    {{ e.button('Mes annonces', url('app_dashboard_advert_index')) }}
{% endblock %}
